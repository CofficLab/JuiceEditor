import TreeNode from "../model/TreeNode"

const quoteNode = new TreeNode({
    uuid: 'codeNode',
    content: `
    <h1 id="heading-1">BlockQuote</h1>

    <blockquote class="my-custom-class"><p>上面这段一般称为：伪代码</p></blockquote>
    `
})

export default quoteNode
